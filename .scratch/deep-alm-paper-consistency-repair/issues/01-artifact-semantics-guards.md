# 01: 产物语义版本与兼容性保护

**What to build:** 使快照、checkpoint、冻结基准、恢复状态及评估/报告消费方验证明确的语义身份，避免形状相同或配置相同就误用旧产物。先提供兼容保护与可信修复状态，不实现后续金融公式。

**Blocked by:** None (can start immediately).

**Status:** resolved

来源：Deep ALM 论文一致性修复增量规格 Revision 1；本专项独立编号，旧专项票不变。

- [x] 集中声明当前实际金融、策略观测、指标及快照语义；登记 C-1/C-2/C-3/C-5/C-6/C-7/C-8/R-1/R-2 的待修复状态。版本与状态由实现生成，不接受调用方宣称完成。
- [x] 保存和读取贯通现有公共接口；缺失、未知、不匹配或伪称全部修复的身份均明确拒绝。旧 checkpoint、恢复、冻结引用、评估和报告不得通过运行目录、设备或形状相同绕过保护。
- [x] 快照仍如实声明当前实际 schema；拒绝不支持或内容/身份不匹配的 schema。存款分组及历史字段的正式 schema 升级归 02/03，不提前宣称 aggregate-only v2 已修复。
- [x] 为后续票提供逐项激活语义版本的边界；本票生成的产物明确属于修复未完成阶段。指标独立升级不必使纯训练权重失效，金融/策略变化则必须使依赖权重失效。
- [x] 保留既有配置、数据哈希、冻结依赖、期限及架构检查；保留历史文件而不修改其元数据。市场原始输入/校准的既有有效复用不被无关报告版本阻塞。
- [x] 在已确认公共 seam 展示失败优先的兼容性回归、同版本往返及阶段状态验收；不写针对私有版本 helper 的唯一测试。
- [x] 按 implement 流程执行针对性测试、可用类型/静态检查、全套测试及 Standards/Spec 审查；不运行八任务训练矩阵。

## Comments

2026-09-08 — 用户已确认十票拆分与阻塞关系；按本地 Markdown tracker 发布。

2026-09-08 — 已实现并验证产物语义保护。`uv run pytest -q` 为 208 passed；未启动八任务训练矩阵。Standards/Spec 审查完成，历史审查报告和 errata 未改动。
